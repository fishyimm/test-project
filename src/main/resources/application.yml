spring:
  h2:
    console: 
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:file:../data/demoDB;DB_CLOSE_ON_EXIT=FALSE
   
  jpa:
    hibernate:
      ddl-auto: update
      
